# Data

The original dataset is **not included** because it contains confidential banking information.

The notebook expects one CSV or Excel file containing the required branch-day fields.
`data_dictionary.csv` documents the expected input columns without publishing any real records.

Do not upload the original raw data to a public GitHub repository.
